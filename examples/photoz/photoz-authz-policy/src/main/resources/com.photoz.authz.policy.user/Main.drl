package com.photoz.authz.policy.admin

import org.keycloak.authz.core.policy.Evaluation;

rule "Authorize View User Album"
    dialect "mvel"
    when
       $evaluation : Evaluation(
           $context : context, $context.identity.hasScope("user")
       )
    then
       $evaluation.grant();
end